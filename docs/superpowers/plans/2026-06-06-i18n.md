# 多语言支持（i18n）实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 为记账 CLI 添加中英文多语言支持，覆盖错误信息、CLI 提示和内置数据。

**架构：** `rust-i18n` 编译期嵌入 YAML 语言文件；`accounting` crate 手动实现 `Display` 调用 `t!`；`accounting-cli` crate 通过 `--lang` 参数切换语言；`accounting-sql` 根据语言选择 seed SQL。

**技术栈：** Rust, rust-i18n v3, YAML, thiserror

---

## 文件变更清单

| 文件 | 职责 | 变更类型 |
|---|---|---|
| `accounting/Cargo.toml` | 添加 rust-i18n 依赖 | 修改 |
| `accounting-cli/Cargo.toml` | 添加 rust-i18n 依赖 | 修改 |
| `accounting/src/lib.rs` | 初始化 i18n | 修改 |
| `accounting/src/error.rs` | 删除 `#[error]`，手动实现 `Display` + `Error` | **大改** |
| `accounting/locales/en.yaml` | 错误信息英文翻译 | 创建 |
| `accounting/locales/zh-CN.yaml` | 错误信息中文翻译 | 创建 |
| `accounting-cli/src/lib.rs` | 新建，初始化 i18n | 创建 |
| `accounting-cli/src/main.rs` | 添加 `--lang` 参数和语言检测 | 修改 |
| `accounting-cli/src/cmd/account.rs` | 中文提示改 `t!` | 修改 |
| `accounting-cli/src/cmd/member.rs` | 中文提示改 `t!` | 修改 |
| `accounting-cli/src/cmd/commodity.rs` | 中文提示改 `t!` | 修改 |
| `accounting-cli/src/cmd/tx.rs` | 中文提示改 `t!` | 修改 |
| `accounting-cli/src/cmd/tag.rs` | 中文提示改 `t!` | 修改 |
| `accounting-cli/src/cmd/report.rs` | 中文提示改 `t!` | 修改 |
| `accounting-cli/locales/en.yaml` | CLI 提示英文翻译 | 创建 |
| `accounting-cli/locales/zh-CN.yaml` | CLI 提示中文翻译 | 创建 |
| `accounting-sql/src/schema.rs` | Seed SQL 拆分为语言版本 | **大改** |
| `accounting-sql/src/impls/sqlite.rs` | `initialize` 方法接收 `lang` | 修改 |

---

## 阶段 1：基础设施

### 任务 1：添加 rust-i18n 依赖

**文件：**
- 修改：`accounting/Cargo.toml`
- 修改：`accounting-cli/Cargo.toml`

- [ ] **步骤 1：修改 `accounting/Cargo.toml`**

在 `[dependencies]` 段末尾追加：

```toml
rust-i18n = "3"
```

- [ ] **步骤 2：修改 `accounting-cli/Cargo.toml`**

在 `[dependencies]` 段末尾追加：

```toml
rust-i18n = "3"
```

- [ ] **步骤 3：验证编译**

```bash
cargo check --workspace
```

预期：编译通过（依赖下载可能需要时间）

- [ ] **步骤 4：Commit**

```bash
git add accounting/Cargo.toml accounting-cli/Cargo.toml
git commit -m "chore(deps): 添加 rust-i18n 依赖"
```

---

### 任务 2：初始化 i18n

**文件：**
- 修改：`accounting/src/lib.rs`
- 创建：`accounting-cli/src/lib.rs`

- [ ] **步骤 1：修改 `accounting/src/lib.rs`**

在文件末尾追加：

```rust
rust_i18n::i18n!("locales", fallback = "en");
```

- [ ] **步骤 2：创建 `accounting-cli/src/lib.rs`**

```rust
pub mod cmd;
pub mod output;

rust_i18n::i18n!("locales", fallback = "en");
```

- [ ] **步骤 3：验证编译**

```bash
cargo check --workspace
```

预期：编译通过

- [ ] **步骤 4：Commit**

```bash
git add accounting/src/lib.rs accounting-cli/src/lib.rs
git commit -m "feat(i18n): 初始化 rust-i18n"
```

---

### 任务 3：创建 accounting 语言文件

**文件：**
- 创建：`accounting/locales/en.yaml`
- 创建：`accounting/locales/zh-CN.yaml`

- [ ] **步骤 1：创建 `accounting/locales/en.yaml`**

```yaml
_version: 1
invalid_transaction: "Invalid transaction: %{msg}"
account_not_found: "Account not found: %{name}"
account_not_empty: "Account has non-zero balance, cannot close: %{name}"
commodity_not_found: "Commodity not found: %{name}"
account_already_exists: "Account already exists: %{name}"
invalid_date: "Invalid date format: %{msg}"
database_error: "Database error: %{msg}"
db_not_initialized: "Database file not found. Please run 'initialize' first."
db_already_exists: "Database file already exists."
unknown_error: "Unknown error: %{msg}"
```

- [ ] **步骤 2：创建 `accounting/locales/zh-CN.yaml`**

```yaml
_version: 1
invalid_transaction: "无效交易: %{msg}"
account_not_found: "账户不存在: %{name}"
account_not_empty: "账户余额非零，无法关闭: %{name}"
commodity_not_found: "商品不存在: %{name}"
account_already_exists: "账户已存在: %{name}"
invalid_date: "日期格式错误: %{msg}"
database_error: "数据库错误: %{msg}"
db_not_initialized: "数据库文件不存在，请先运行 initialize 命令"
db_already_exists: "数据库文件已存在"
unknown_error: "未知错误: %{msg}"
```

- [ ] **步骤 3：Commit**

```bash
git add accounting/locales/
git commit -m "feat(i18n): 创建 accounting 错误信息语言文件"
```

---

## 阶段 2：核心错误信息改造

### 任务 4：改造 AccountingError

**文件：**
- 修改：`accounting/src/error.rs`

- [ ] **步骤 1：重写 `AccountingError`**

删除所有 `#[error("...")]` 属性，删除 `use thiserror::Error;`，改为：

```rust
use rust_i18n::t;

/// 核心库错误类型
#[derive(Debug, Clone, PartialEq)]
pub enum AccountingError {
    /// 交易校验失败
    InvalidTransaction(String),
    /// 指定账户不存在
    AccountNotFound(String),
    /// 账户余额非零导致无法关闭
    AccountNotEmpty(String),
    /// 指定商品不存在
    CommodityNotFound(String),
    /// 账户已存在
    AccountAlreadyExists(String),
    /// 日期解析失败
    InvalidDate(String),
    /// 底层数据库异常
    DatabaseError(String),
    /// 数据库文件不存在
    DbNotInitialized,
    /// 数据库文件已存在
    DbAlreadyExists,
    /// 其他未知错误
    Unknown(String),
}

impl std::fmt::Display for AccountingError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::InvalidTransaction(msg) => {
                write!(f, "{}", t!("invalid_transaction", msg = msg))
            }
            Self::AccountNotFound(name) => {
                write!(f, "{}", t!("account_not_found", name = name))
            }
            Self::AccountNotEmpty(name) => {
                write!(f, "{}", t!("account_not_empty", name = name))
            }
            Self::CommodityNotFound(name) => {
                write!(f, "{}", t!("commodity_not_found", name = name))
            }
            Self::AccountAlreadyExists(name) => {
                write!(f, "{}", t!("account_already_exists", name = name))
            }
            Self::InvalidDate(msg) => {
                write!(f, "{}", t!("invalid_date", msg = msg))
            }
            Self::DatabaseError(msg) => {
                write!(f, "{}", t!("database_error", msg = msg))
            }
            Self::DbNotInitialized => {
                write!(f, "{}", t!("db_not_initialized"))
            }
            Self::DbAlreadyExists => {
                write!(f, "{}", t!("db_already_exists"))
            }
            Self::Unknown(msg) => {
                write!(f, "{}", t!("unknown_error", msg = msg))
            }
        }
    }
}

impl std::error::Error for AccountingError {}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_error_display() {
        let err = AccountingError::InvalidTransaction("unbalanced".to_string());
        assert!(err.to_string().contains("unbalanced"));
    }
}
```

- [ ] **步骤 2：验证编译**

```bash
cargo test -p accounting
```

预期：30 passed, 0 failed

- [ ] **步骤 3：Commit**

```bash
git add accounting/src/error.rs
git commit -m "feat(i18n): AccountingError 改为手动 Display，支持多语言"
```

---

## 阶段 3：CLI 提示改造

### 任务 5：创建 CLI 语言文件

**文件：**
- 创建：`accounting-cli/locales/en.yaml`
- 创建：`accounting-cli/locales/zh-CN.yaml`

- [ ] **步骤 1：创建 `accounting-cli/locales/en.yaml`**

```yaml
_version: 1
db_initialized: "Database initialized"
tx_created: "Transaction created, ID: %{id}"
tx_not_found: "Transaction not found: %{id}"
tx_deleted: "Transaction deleted, ID: %{id}"
tx_updated: "Transaction updated, ID: %{id}"
member_created: "Member created, ID: %{id}"
member_deleted: "Member deleted, ID: %{id}"
tag_created: "Tag created, ID: %{id}"
tag_deleted: "Tag deleted: %{name}"
commodity_created: "Commodity created, ID: %{id}"
account_created: "Account created, ID: %{id}"
account_not_found: "Account not found: %{id}"
account_closed: "Account closed, ID: %{id}"
account_reopened: "Account reopened, ID: %{id}"
balance_zero: "Balance is zero"
no_data: "No data"
```

- [ ] **步骤 2：创建 `accounting-cli/locales/zh-CN.yaml`**

```yaml
_version: 1
db_initialized: "数据库已初始化"
tx_created: "交易已创建，ID: %{id}"
tx_not_found: "交易不存在: %{id}"
tx_deleted: "交易已删除，ID: %{id}"
tx_updated: "交易已更新，ID: %{id}"
member_created: "成员已创建，ID: %{id}"
member_deleted: "成员已删除，ID: %{id}"
tag_created: "标签已创建，ID: %{id}"
tag_deleted: "标签已删除: %{name}"
commodity_created: "商品已创建，ID: %{id}"
account_created: "账户已创建，ID: %{id}"
account_not_found: "账户不存在: %{id}"
account_closed: "账户已关闭，ID: %{id}"
account_reopened: "账户已重新开启，ID: %{id}"
balance_zero: "余额为零"
no_data: "暂无数据"
```

- [ ] **步骤 3：Commit**

```bash
git add accounting-cli/locales/
git commit -m "feat(i18n): 创建 CLI 提示语言文件"
```

---

### 任务 6：改造 main.rs（语言切换）

**文件：**
- 修改：`accounting-cli/src/main.rs`
- 修改：`accounting-cli/src/cmd/mod.rs`

- [ ] **步骤 1：修改 `accounting-cli/src/cmd/mod.rs` 添加 `--lang` 参数**

```rust
#[derive(Parser)]
#[command(version, about)]
pub struct Cli {
    /// 数据库文件路径
    pub db: PathBuf,
    /// 输出格式
    #[arg(long, value_enum, default_value = "table")]
    pub format: OutputFormat,
    /// 语言（如 zh-CN、en）
    #[arg(long)]
    pub lang: Option<String>,
    #[command(subcommand)]
    pub command: Commands,
}
```

- [ ] **步骤 2：重写 `accounting-cli/src/main.rs`**

```rust
//! CLI 工具：基于 clap 的命令行记账工具

mod cmd;
mod output;

use clap::Parser;
use cmd::{Cli, Commands};
use std::process;

fn detect_language(cli_lang: Option<&str>) -> String {
    if let Some(lang) = cli_lang {
        return lang.to_string();
    }
    if let Ok(lang) = std::env::var("LANG") {
        return lang.split('.').next().unwrap_or("en").to_string();
    }
    "en".to_string()
}

#[tokio::main]
async fn main() {
    let cli = Cli::parse();
    let lang = detect_language(cli.lang.as_deref());
    rust_i18n::set_locale(&lang);

    let result = match &cli.command {
        Commands::Initialize => initialize(&cli.db).await,
        _ => run_command(cli).await,
    };

    if let Err(e) = result {
        eprintln!("错误: {}", e);
        process::exit(1);
    }
}

async fn initialize(db_path: &std::path::Path) -> Result<(), accounting::error::AccountingError> {
    if db_path.exists() {
        return Err(accounting::error::AccountingError::DbAlreadyExists);
    }
    let db = accounting_sql::impls::sqlite::SqliteDatabase::open(db_path.to_str().unwrap())
        .map_err(|e| accounting::error::AccountingError::Unknown(e.to_string()))?;
    println!("{}", rust_i18n::t!("db_initialized"));
    Ok(())
}

async fn run_command(cli: Cli) -> Result<(), accounting::error::AccountingError> {
    if !cli.db.exists() {
        return Err(accounting::error::AccountingError::DbNotInitialized);
    }
    let db = accounting_sql::impls::sqlite::SqliteDatabase::open(cli.db.to_str().unwrap())
        .map_err(|e| accounting::error::AccountingError::Unknown(e.to_string()))?;

    match cli.command {
        Commands::Initialize => unreachable!(),
        Commands::Member(cmd) => cmd.run(db, cli.format).await,
        Commands::Account(cmd) => cmd.run(db, cli.format).await,
        Commands::Commodity(cmd) => cmd.run(db, cli.format).await,
        Commands::Tx(cmd) => cmd.run(db, cli.format).await,
        Commands::Tag(cmd) => cmd.run(db, cli.format).await,
        Commands::Report(cmd) => cmd.run(db, cli.format).await,
    }
}
```

- [ ] **步骤 3：验证编译**

```bash
cargo check -p accounting-cli
```

预期：编译通过

- [ ] **步骤 4：Commit**

```bash
git add accounting-cli/src/main.rs accounting-cli/src/cmd/mod.rs
git commit -m "feat(i18n): CLI 添加 --lang 参数和语言检测"
```

---

### 任务 7：改造 cmd/*.rs（中文提示改 t!）

**文件：**
- 修改：`accounting-cli/src/cmd/account.rs`
- 修改：`accounting-cli/src/cmd/member.rs`
- 修改：`accounting-cli/src/cmd/commodity.rs`
- 修改：`accounting-cli/src/cmd/tx.rs`
- 修改：`accounting-cli/src/cmd/tag.rs`
- 修改：`accounting-cli/src/cmd/report.rs`

- [ ] **步骤 1：修改 `accounting-cli/src/cmd/account.rs`**

将所有 `print_line(&format!("中文...", ...), format)` 替换为 `print_line(&format!("{}", t!("key", ...)), format)`：

```rust
// 账户已创建，ID: {}
print_line(&format!("{}", t!("account_created", id = id.0)), format);

// 账户不存在: {}
print_line(&format!("{}", t!("account_not_found", id = args.id)), format);

// 账户已关闭，ID: {}
print_line(&format!("{}", t!("account_closed", id = args.id)), format);

// 账户已重新开启，ID: {}
print_line(&format!("{}", t!("account_reopened", id = args.id)), format);

// 余额为零
print_line(&t!("balance_zero").to_string(), format);
```

- [ ] **步骤 2：修改 `accounting-cli/src/cmd/member.rs`**

```rust
print_line(&format!("{}", t!("member_created", id = id.0)), format);
print_line(&format!("{}", t!("member_deleted", id = args.id)), format);
```

- [ ] **步骤 3：修改 `accounting-cli/src/cmd/commodity.rs`**

```rust
print_line(&format!("{}", t!("commodity_created", id = id.0)), format);
```

- [ ] **步骤 4：修改 `accounting-cli/src/cmd/tx.rs`**

```rust
print_line(&format!("{}", t!("tx_created", id = id.0)), format);
print_line(&format!("{}", t!("tx_not_found", id = args.id)), format);
print_line(&format!("{}", t!("tx_deleted", id = args.id)), format);
print_line(&format!("{}", t!("tx_updated", id = id)), format);
```

- [ ] **步骤 5：修改 `accounting-cli/src/cmd/tag.rs`**

```rust
print_line(&format!("{}", t!("tag_created", id = id.0)), format);
print_line(&format!("{}", t!("tag_deleted", name = args.name)), format);
```

- [ ] **步骤 6：修改 `accounting-cli/src/cmd/report.rs`**

```rust
print_line(&t!("balance_zero").to_string(), format);
print_line(&t!("no_data").to_string(), format);
```

- [ ] **步骤 7：验证编译**

```bash
cargo check -p accounting-cli
```

预期：编译通过

- [ ] **步骤 8：Commit**

```bash
git add accounting-cli/src/cmd/
git commit -m "feat(i18n): CLI 提示信息全部改为 t! 宏"
```

---

## 阶段 4：Seed 数据改造

### 任务 8：改造 schema.rs 和 sqlite.rs

**文件：**
- 修改：`accounting-sql/src/schema.rs`
- 修改：`accounting-sql/src/impls/sqlite.rs`

- [ ] **步骤 1：修改 `accounting-sql/src/schema.rs`**

将 `SEED_SQL` 常量拆分为语言相关版本：

```rust
const SEED_ACCOUNTS_EN: &str = r#"
INSERT OR IGNORE INTO accounts (full_name, account_type, parent_id, is_system) VALUES
('Equity:OpeningBalances', 3, NULL, 1),
('Income:Uncategorized', 4, NULL, 1),
('Expenses:Uncategorized', 5, NULL, 1),
('Expenses:Fees', 5, NULL, 1),
('Expenses:Discounts', 5, NULL, 1),
('Expenses:InstallmentFees', 5, NULL, 1),
('Assets:Cashback', 1, NULL, 1);
"#;

const SEED_ACCOUNTS_ZH: &str = r#"
INSERT OR IGNORE INTO accounts (full_name, account_type, parent_id, is_system) VALUES
('权益:期初余额', 3, NULL, 1),
('收入:未分类', 4, NULL, 1),
('费用:未分类', 5, NULL, 1),
('费用:手续费', 5, NULL, 1),
('费用:折扣', 5, NULL, 1),
('费用:分期手续费', 5, NULL, 1),
('资产:返现', 1, NULL, 1);
"#;

const SEED_COMMODITIES: &str = r#"
INSERT OR IGNORE INTO commodities (symbol, name, precision) VALUES ('CNY', '人民币', 2);
"#;

const SEED_TAGS_EN: &str = r#"
INSERT OR IGNORE INTO tags (name, description, is_system) VALUES
('repayment', 'Installment or credit card repayment marker', 1);
"#;

const SEED_TAGS_ZH: &str = r#"
INSERT OR IGNORE INTO tags (name, description, is_system) VALUES
('repayment', '分期/信用卡还款标记', 1);
"#;

/// 插入系统内置数据，支持语言选择
pub fn insert_seed_data(conn: &Connection, lang: &str) -> Result<(), DbError> {
    let accounts_sql = if lang.starts_with("zh") {
        SEED_ACCOUNTS_ZH
    } else {
        SEED_ACCOUNTS_EN
    };
    let tags_sql = if lang.starts_with("zh") {
        SEED_TAGS_ZH
    } else {
        SEED_TAGS_EN
    };
    conn.execute_batch(&format!("{}{}{}", SEED_COMMODITIES, accounts_sql, tags_sql))?;
    Ok(())
}
```

同时更新测试中的 `insert_seed_data` 调用：

```rust
// test_schema_initialization 中
insert_seed_data(&conn, "en").unwrap();

// test_seed_data 中
insert_seed_data(&conn, "en").unwrap();
```

- [ ] **步骤 2：修改 `accounting-sql/src/impls/sqlite.rs`**

找到 `initialize_schema` 和 `insert_seed_data` 的调用，确保 `insert_seed_data` 接收 `lang` 参数。由于 `sqlite.rs` 主要是测试代码，需要检查是否有直接调用 `insert_seed_data` 的地方，如果有，传入 `"en"`。

如果 `SqliteDatabase` 有 `initialize` 方法，修改其签名：

```rust
pub fn initialize(&self, lang: &str) -> Result<(), DbError> {
    initialize_schema(&self.conn)?;
    insert_seed_data(&self.conn, lang)?;
    Ok(())
}
```

- [ ] **步骤 3：验证编译**

```bash
cargo test -p accounting-sql
```

预期：39 passed, 0 failed

- [ ] **步骤 4：Commit**

```bash
git add accounting-sql/src/schema.rs accounting-sql/src/impls/sqlite.rs
git commit -m "feat(i18n): Seed 数据支持语言选择"
```

---

## 阶段 5：集成验证

### 任务 9：全量编译和测试

- [ ] **步骤 1：运行 fmt**

```bash
cargo fmt --all
```

- [ ] **步骤 2：运行全量测试**

```bash
cargo test --workspace
```

预期：82 passed, 0 failed

- [ ] **步骤 3：运行 clippy**

```bash
cargo clippy --workspace -- -D warnings
```

预期：0 warnings

- [ ] **步骤 4：Commit**

```bash
git add -A
git commit -m "feat(i18n): 全量集成验证通过"
```

---

### 任务 10：多语言端到端测试

**文件：**
- 修改：`accounting/src/error.rs`（测试模块）

- [ ] **步骤 1：添加翻译测试**

在 `accounting/src/error.rs` 的 `tests` 模块中追加：

```rust
    #[test]
    fn test_error_display_en() {
        rust_i18n::set_locale("en");
        let err = AccountingError::DbNotInitialized;
        assert!(err.to_string().contains("not found"));
    }

    #[test]
    fn test_error_display_zh() {
        rust_i18n::set_locale("zh-CN");
        let err = AccountingError::DbNotInitialized;
        assert!(err.to_string().contains("不存在"));
    }
```

- [ ] **步骤 2：验证测试**

```bash
cargo test -p accounting error_display
```

预期：测试通过

- [ ] **步骤 3：Commit**

```bash
git add accounting/src/error.rs
git commit -m "test(i18n): 添加中英文错误信息翻译测试"
```

---

## 风险与回退

| 风险 | 缓解措施 |
|---|---|
| `rust-i18n` 与 `thiserror` 宏冲突 | 手动实现 `Display` + `Error`，不依赖 `#[error]` |
| `t!` 宏在 `Display::fmt` 中无法编译 | 若编译失败，回退到"核心库纯英文 + CLI 层翻译"方案 |
| 语言文件路径错误导致编译失败 | `i18n!("locales")` 路径相对于 crate 根目录，确保 `locales/` 与 `src/` 同级 |
| Seed 数据拆分后 schema 测试失败 | 所有测试统一传入 `"en"`，保证测试环境一致 |
| `rust_i18n::set_locale` 影响全局状态 | 单线程 CLI 无并发问题；测试中按顺序设置 locale 即可 |

---

## 执行交接

计划已完成并保存到 `docs/superpowers/plans/2026-06-06-i18n.md`。

**执行方式：子代理驱动**

按任务 1 → 10 的顺序逐个调度子代理。每个子代理完成后在当前会话中验证 `cargo test`，通过后再调度下一个。